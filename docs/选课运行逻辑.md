## 🚀 代码运行流程

### 1. 程序启动阶段

```
main() → setup_logger() → print_welcome() → get_user_config()
```

### 2. 登录认证阶段

```
simulate_login() → handle_captcha() → login() → 获取cookie和session
```

### 3. 选课执行阶段

```
get_jx0502zbid() → select_courses() → search_and_select_course() → 发送选课请求
```

### 4. 通知反馈阶段

```
dingtalk() / feishu() → 发送选课结果通知
```

## �� 关键请求函数

### **认证相关请求**

#### 1. 验证码获取

```python
# 文件: main.py - handle_captcha()
URL: "http://zhjw.qfnu.edu.cn/jsxsd/verifycode.servlet"
方法: GET
功能: 获取登录验证码图片
```

#### 2. 用户登录

```python
# 文件: main.py - login()
URL: "http://zhjw.qfnu.edu.cn/jsxsd/xk/LoginToXkLdap"
方法: POST
功能: 执行用户登录认证
参数: userAccount, userPassword, RANDOMCODE, encoded
```

### **选课系统请求**

#### 3. 获取选课轮次 ID

```python
# 文件: src/core/course_selector.py - get_jx0502zbid()
URL: "http://zhjw.qfnu.edu.cn/jsxsd/xsxk/xklc_list"
方法: GET
功能: 获取当前可用的选课轮次编号
```

#### 4. 访问选课页面

```python
# 文件: main.py - select_courses()
URL: "http://zhjw.qfnu.edu.cn/jsxsd/xsxk/xsxk_index?jx0502zbid={id}"
方法: GET
功能: 进入选课系统主页面
```

#### 5. 获取选修选课课程列表

```python
# 文件: src/core/course_selector.py - get_xxxk_course_list()
URL: "http://zhjw.qfnu.edu.cn/jsxsd/xsxkkc/xsxkXxxk"
方法: POST
功能: 获取可选课程列表
```

### **选课操作请求**

#### 6. 公选课选课

```python
# 文件: src/core/send_course_data.py - send_ggxxkxkOper_course_jx02id_and_jx0404id()
URL: "http://zhjw.qfnu.edu.cn/jsxsd/xsxkkc/ggxxkxkOper"
方法: GET
参数: kcid, cfbs, jx0404id, xkzy, trjf
功能: 执行公选课选课操作
```

#### 7. 专业内跨年级选课

```python
# 文件: src/core/send_course_data.py - send_knjxkOper_course_jx02id_and_jx0404id()
URL: "http://zhjw.qfnu.edu.cn/jsxsd/xsxkkc/knjxkOper"
方法: GET
参数: kcid, cfbs, jx0404id, xkzy, trjf
功能: 执行专业内跨年级选课操作
```

#### 8. 本学期计划选课

```python
# 文件: src/core/send_course_data.py - send_bxqjhxkOper_course_jx02id_and_jx0404id()
URL: "http://zhjw.qfnu.edu.cn/jsxsd/xsxkkc/bxqjhxkOper"
方法: GET
参数: kcid, cfbs, jx0404id, xkzy, trjf
功能: 执行本学期计划选课操作
```

#### 9. 选修选课

```python
# 文件: src/core/send_course_data.py - send_xxxkOper_course_jx02id_and_jx0404id()
URL: "http://zhjw.qfnu.edu.cn/jsxsd/xsxkkc/xxxkOper"
方法: GET
参数: kcid, cfbs, jx0404id, xkzy, trjf
功能: 执行选修选课操作
```

#### 10. 计划外选课

```python
# 文件: src/core/send_course_data.py - send_fawxkOper_course_jx02id_and_jx0404id()
URL: "http://zhjw.qfnu.edu.cn/jsxsd/xsxkkc/fawxkOper"
方法: GET
参数: kcid, cfbs, jx0404id, xkzy, trjf
功能: 执行计划外选课操作
```

### **系统访问请求**

#### 11. 教务系统首页

```python
# 文件: main.py - simulate_login()
URL: "http://zhjw.qfnu.edu.cn/jsxsd/"
方法: GET
功能: 获取系统cookie和建立会话
```

#### 12. 用户主页

```python
# 文件: main.py - main()
URL: "http://zhjw.qfnu.edu.cn/jsxsd/framework/xsMain.jsp"
方法: GET
功能: 访问用户主页面
```

## 选课模式

### **三种选课模式**

1. **fast 模式**: 高速模式，最快速度持续尝试选课
2. **normal 模式**: 普通模式，正常速度选课，每次请求间隔 5 秒
3. **snipe 模式**: 蹲课模式，每次选课前刷新轮次，持续执行选课操作

## 🔄 核心流程循环

```
while True:
    1. 登录认证
    2. 获取选课轮次ID
    3. 进入选课系统
    4. 执行选课操作
    5. 检查选课结果
    6. 发送通知
    7. 根据模式决定是否继续循环
```

## 关键特点

- **多选课方式**: 支持 5 种不同的选课类型
- **智能重试**: 自动重试登录和选课操作
- **实时通知**: 通过钉钉和飞书机器人发送选课结果
- **灵活配置**: 支持手动配置课程 ID 或自动搜索课程
- **会话管理**: 统一的 session 管理，支持 cookie 持久化

这套代码实现了一个完整的教务系统自动选课流程，从登录认证到选课操作再到结果通知，形成了一个闭环的自动化系统。
